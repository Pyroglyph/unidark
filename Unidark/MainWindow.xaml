<Window x:Class="Unidark.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Unidark"
        mc:Ignorable="d"
        Topmost="True"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Unidark"
        Height="70" Width="420"
        Loaded="MainWindow_OnLoaded"
        Background="#444" BorderBrush="#333" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" x:Name="lblStatus" Margin="6 6 6 0"  VerticalContentAlignment="Center" 
                Foreground="#ccc" FontSize="14" />
        <ProgressBar Grid.Row="1" x:Name="pbarStage" Margin="6 0 6 6" 
                Background="#333" BorderBrush="{x:Null}" Foreground="#555"/>
    </Grid>
</Window>
